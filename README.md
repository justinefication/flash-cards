# Flash Card

A flash card app focused on state management and component-based architecture using Vue.
Learn more on [Roadmap.sh](https://roadmap.sh/projects/flash-cards)

## Running the application

1. Install dependencies
    ```bash
    pnpm install
    ```

2. Run the application
    ```bash
    pnpm run dev
    ```
